# Calculate quantiles for discrete Y by bisection

Used in 'qPr()'.

## Usage

``` r
.qYXdiscr(
  iyx,
  params1,
  params2,
  auxmetadata,
  temporarydir,
  usememory = TRUE,
  doquantiles,
  quantiles,
  dosamples,
  nsamples,
  Qerror = NULL,
  tol = NULL
)
```
