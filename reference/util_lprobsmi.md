# Calculate and combine log-probabilities

Calculate log2_p(Y1\|Y2), log2_p(Y2\|Y1), log2_p(Y1), log2_p(Y2) for one
datapoint. Used in mutualinfo().

## Usage

``` r
util_lprobsmi(xVs, params1, params2, lWnorm, lW)
```
