% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/display.R
\name{print.eutility}
\alias{print.eutility}
\title{Print an object of class "eutility" (expected utility)}
\usage{
\method{print}{eutility}(x, elements = NULL, digits = TRUE, edigits = 2, ...)
}
\arguments{
\item{x}{Object of class "eutility", obtained with \code{\link[=exputility]{exputility()}}.}

\item{elements}{character or integer vector, or \code{NULL} (default): elements of the "expected utility" object to display. The syntax is the same as with \code{\link[base:Extract]{[}}. If \code{NULL}, the elements \code{'value'}, \code{'value.acc'}, \code{'optimal.probs'} are displayed together in a special way.}

\item{digits}{positive integer or \code{NULL} or \code{TRUE} (default): minimal number of significant digits, see \code{\link[base:print.default]{base::print.default()}}. If value is \code{TRUE}, then the significant digits for element \code{'value'} are determined from is respective \code{'value.acc'}  (see \code{\link[=exputility]{exputility()}}), according to the rules of the \emph{Guide to the expression of Uncertainty in Measurement}, keeping as many digits as given in parameter \code{edigits}.}

\item{edigits}{positive integer, default 2: number of significant digits for element \code{'value'} and \code{'quantiles'}, if \code{digits = TRUE}.}

\item{...}{Other parameters to be passed to \code{\link[base:print]{base::print()}}.}
}
\value{
Its \code{x} argument, \link[base:invisible]{invisibly}; see \code{\link[base:print]{base::print()}}.
}
\description{
This \code{\link[base:print]{base::print()}} method is a utility to display value and revisability of an "mi" object obtained with \code{\link[=mutualinfo]{mutualinfo()}}.
}
\examples{
## Use the example "Knowledge" object 'Kexample'
## calculated from the "penguins" dataset;
## variates: 'species' and 'bill_len'

## define a utility matrix with four actions,
## and outcomes depending on the variate 'species'
umatrix <- matrix(c(
 1.80, 0.42, 1.60, -0.12, -1.10, 0.20, -0.51, 0.35, -0.49, 0.35, -0.48, 0.62
 ), nrow = 4, ncol = 3, dimnames = list(actions = paste0('A', 1:4), NULL))

print(umatrix)

## Calculate the probability of the 'species outcomes
probs <- Pr(data.frame(species = c('Adelie', 'Chinstrap', 'Gentoo')),
  Kexample)

## Calculate the expected utilities of the actions
eu <- exputility(umatrix, probs)

## Print the expected utility of each action, its numerical accuracy,
## and the probability that it would be optimal if more data were available

print(eu)

}
\references{
\itemize{
\item Joint Committee for Guides in Metrology (2008): \emph{Guide to the expression of uncertainty in measurement}, \url{doi:10.59161/JCGM100-2008E}, \url{https://www.iso.org/sites/JCGM/GUM-JCGM100.htm}.
}
}
\seealso{
\code{\link[=exputility]{exputility()}} to calculate expected utilities and their revisability.
}
\concept{display}
