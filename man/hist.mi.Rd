% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/display.R
\name{hist.mi}
\alias{hist.mi}
\title{Plot the revisability of an object of class "mi" as a histogram}
\usage{
\method{hist}{mi}(
  x,
  breaks = NULL,
  lty = c(1, 2, 4, 3, 6, 5),
  lwd = 2,
  col = palette(),
  alpha.f = 1,
  alpha.f.fill = 0.125,
  showvalue = TRUE,
  xlab = NULL,
  ylab = NULL,
  xlim = NULL,
  ylim = c(0, NA),
  main = NULL,
  grid = TRUE,
  axes = FALSE,
  add = FALSE,
  ...
)
}
\arguments{
\item{x}{Object of class "mi", obtained with \code{\link[=mutualinfo]{mutualinfo()}}.}

\item{breaks}{as in function \code{\link[graphics:hist]{graphics::hist()}}, or \code{NULL} (default). Value \code{NULL} uses a number of bins inversely proportional to the square root of the number of samples (because the Monte Carlo error associated with the quantiles scales as the square root).}

\item{lty, lwd, col, alpha.f, xlab, ylab, xlim, ylim, main, grid, axes, add}{see analogous arguments in \code{\link[graphics:matplot]{graphics::matplot()}}}

\item{alpha.f.fill}{Numeric, default 0.125: opacity of the histogram filling. \code{0} means no filling.}

\item{showvalue}{Logical, default \code{TRUE}: show the mutual information obtained from the current data sample?}

\item{...}{Other parameters to be passed to \code{\link[=pplot]{pplot()}}.}
}
\value{
\link[base:invisible]{Invisibly}, an object of class \link[graphics:hist]{"histogram"}.
}
\description{
The mutual information calculated with the \code{\link[=mutualinfo]{mutualinfo()}} function, and outputted as a "mi" object, has an associated "revisability" that comes from the finite size of the data sample. A much larger sample might reveal a different value of mutual information.

The \code{hist()} method for a "mi" object is a utility to visualize this kind of revisability, in the form of a distribution: it shows how the mutual information could change, if we collected a much larger (infinite) data sample, and how likely would such change be.
}
\examples{
## Load the example `K`nowledge object calculated from the "penguins" dataset;
## variates: 'species' and 'bill_len'
K <- Kexample

## calculate the mutual information and its revisability
MI <- mutualinfo(Y1names = 'species', Y2names = 'bill_len', K = K, nv = 2)

## show the possible revisability of the mutual information,
## if a much larger data sample were collected
hist(MI)

}
\seealso{
\code{\link[=mutualinfo]{mutualinfo()}} to calculate mutual information and its revisability.

\code{\link[=print.mi]{print.mi()}} ] to plot mutual information and quantiles calculated by \code{mutualinfo()}

\code{\link[=pplot]{pplot()}} (on which \code{hist.mi()} is based) for more general plots.
}
\concept{display}
